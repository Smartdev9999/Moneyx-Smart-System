# Memory Index

(append) v2.8.7: [Purge Inputs + Reserve Profit Fix](mem://trading/golden2-ea/v2-8-7-purge-inputs-and-reserve-profit-fix) — deletes deprecated multi-confirm Squeeze inputs/globals/handles; replaces broken MinGain-delta HOLD gate with `winProfit >= ReserveProfitUSD + MinGainUSD`; shred passes use `InpExit_ReserveProfitUSD` as actual KEEP floor (was hardcoded MinNetUSD=$1)

(append) v2.9.2: [Hedge-Used Advance Bypass + Stale Opposite-Side Pending Cleanup](mem://trading/golden2-ea/v2-9-2-hedge-advance-bypass-and-stale-pending-cleanup) — IsPriorGroupSafeForAdvance / IsGroupSafeToAdvance now safe-pass on g_groupHedgeUsed alone (no longer requires PostMatchAvgActive) → unblocks queue freeze when every prior sits in Hedge USED/LOCKED PostAvg:WAITING. Mirror cleanup adds order-type side filter; ManageGroupHedgeArm sweeps opposite-side hedge pendings before early-return; OnTick group loop full-sweeps hedge pendings once post-match active or hedge positions gone — fixes stale SELL_STOP G6_S_HD_GL pendings that survived hedge side flip.

(append) v2.9.3: [Disarm Partial-Fill + DD Diagnostic](mem://trading/golden2-ea/v2-9-3-disarm-partial-fill) — HD pending disarm now also fires when some hedge positions already filled: if `pct < InpHedgeDisarmPercent` and hedge pendings still exist while `hedgePosExists==true`, remaining pendings are deleted (filled HD positions kept for Triple-Gate/Recovery). Adds per-group 10s-throttled `HD DISARM-CHK` verbose log (pct, arm%, disarm%, hPend, hPos, lossSide). Fixes "disarm never triggers" when ladder partially filled then DD recovered.

(append) v2.9.4: [Max Lot Caps + Max DD Close](mem://trading/golden2-ea/v2-9-4-max-lot-and-max-dd-close) — adds two **independent** lot caps: `InpMaxLotPerOrder` (NORMAL entry/grid/hedge mirror via `CapNormalLotG2` wrapping `LotForLevel`/`ResolveLot`/`EntryInitialLotG2`) and `InpMaxLotTripleGate` (TRIPLE-GATE exit RC#N recovery only via `CapTripleGateLotG2`). Adds global Max DD Close kill switch (`InpMaxDDMode` OFF/PERCENT/DOLLAR + `InpMaxDDValue`): `ManageMaxDDClose()` runs every tick before per-group loop, 30s cooldown, flattens all EA positions + deletes all EA pendings when floating loss exceeds threshold. Dashboard shows two new rows. Zero changes to strategy logic.
